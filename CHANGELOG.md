# 更新记录

## 1.0.2

- 预览恢复黑色背景及白色导航、工具栏文字，返回相册列表时恢复选择页主题。
- 新增 `HXMediaPicker.defaultAppearance`，可统一设置相册选择页的自动、浅色或深色外观；单次 `options.appearance` 设置优先，独立相机默认外观不受影响。
- 支持控制器管理或应用管理状态栏；跟随页面和主题变化更新样式，关闭时在宿主回调前恢复原状态栏。
- 保留经典导航、裁剪布局和微信绿色选中配色，继续依赖 HXPhotoPicker `5.0.6`。

## 1.0.1

- 新增默认关闭的 `saveOriginalPhotoBeforeEditing`，允许独立相机在照片编辑前保存原片。
- 该模式下取消编辑返回相机，保留已经保存的原片；完成编辑交付编辑结果，不重复保存。
- 宿主直接关闭保存原片后的相机或编辑页面时，释放选择器会话；其他全屏页面覆盖相机不会误触发取消。
- 默认配置保持原有保存时机，继续依赖 HXPhotoPicker `5.0.6`。

## 1.0.0

- 提供 Swift 和 Objective-C 统一的媒体选择、相机及基础裁剪入口。
- 支持照片多选与视频单选，返回图片或本地视频 URL。
- 支持自动、浅色、深色外观及选中状态主色配置。
- 使用经典基础裁剪布局、普通导航控件和系统预览切换。
- 提供主线程生命周期与结果回调，处理图片读取和视频导出中间文件。
- 提供独立的 Objective-C / Swift 测试工程。
- 通过公开 Git tag 分发，依赖 `wowbby/HXPhotoPicker` fork 的 `5.0.6`，尚未发布到 CocoaPods Trunk。

已知限制：经典裁剪框内部整体拖动尚未实现；启用拍照编辑及相册保存时保存裁剪结果；HXPhotoPicker 编辑大图前可能降采样。详情见 [README](README.md)。
